# 2026-06-14 — RAG V1 (벡터DB + top-k 근거 생성)

## 목표
pgvector + 임베딩 + top-k 검색으로 quiz-gen을 **근거 기반(환각↓)**으로. 무료(Supabase pgvector + Gemini 임베딩/생성).

## 범위
- **포함(V1)**: 018(vector·doc_chunks·match_chunks), `/api/embed-cards`(카드 임베딩), `/api/quiz-gen` 업그레이드(top-k 근거 + 카드 폴백), QuizAdmin 임베딩 버튼. 임베딩 = `text-embedding-004`(768, 무료).
- **제외**: PDF 업로드(V2), HWP→PDF(V3), B-2 RAG 상담(V3), 출처표시·리랭크(V4).

## 결과 (완료 2026-06-14)
- 파일: `supabase/018_vector.sql`, `api/_ai.js`, `api/embed-cards.js`, `api/quiz-gen.js`(업글), `lib/quiz.ts`(embedDeck·generateQuestions), `QuizAdmin`(임베딩 버튼).
- check + build + 함수 구문 통과. 배포.
- **운영 필수**: Supabase에서 **018 실행** (`create extension vector` 포함).

## 사용 흐름
1. 교수 → CBT 문제 관리 → 분야 선택 → **"이 분야 임베딩 생성/갱신"**
2. 토픽 선택 → **✨ AI 생성** → top-k 근거로 생성(grounded) → draft → 검증

## 후속
- V2: PDF 업로드(`/api/ingest`, 클라 텍스트 추출) → doc_chunks(source=pdf)
- V3: HWP(→PDF 권장) + **B-2 RAG 상담**(`/api/ask`, match_chunks 재사용)
- V4: 출처표시·청킹/리랭크 튜닝, 임베딩 모델 업그레이드(gemini-embedding-001)
